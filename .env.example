PORT=3000
ADMIN_PASSWORD=changeme123

# Supabase (for persistent storage). Leave these blank to use a local JSON file
# instead. Get the values from Supabase -> Project Settings -> API.
# Use the SECRET key (starts with sb_secret_), not the publishable one.
SUPABASE_URL=
SUPABASE_KEY=
